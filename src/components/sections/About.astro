---
import AboutHeading from "@/components/animations/about/AboutHeading.astro";
import Glaciers from "@/components/animations/about/Glaciers.astro";
import SectionWrapper from "@/components/layout/SectionWrapper.astro";
import { cn } from "@/lib/utils";

interface Props {
  className?: string;
  id?: string;
}

const { className, id } = Astro.props;
---

<SectionWrapper id={id} className={cn("relative overflow-hidden", className)}>
  <AboutHeading />

  <Glaciers />
</SectionWrapper>
