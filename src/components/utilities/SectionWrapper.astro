---
import { cn } from "@/lib/utils";

const { elementType = "section", className, ...props } = Astro.props;
const SectionElement = elementType;
---

<SectionElement
  class={cn(
    "sm:px-4 h-svh max-h-[1400px] min-h-[750px] md:px-6 lg:px-16 w-full relative",
    className,
  )}
  {...props}
>
  <slot />
</SectionElement>
