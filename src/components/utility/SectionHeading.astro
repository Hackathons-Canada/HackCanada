---
import { cn } from "@/lib/utils";

interface Props {
  className?: string;
  id?: string;
}

const { className, id } = Astro.props;
---

<h2
  class={cn("heading-font text-6xl font-bold md:text-7xl py-6", className)}
  id={id}
>
  <slot />
</h2>
