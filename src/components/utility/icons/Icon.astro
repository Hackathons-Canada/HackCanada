---
import { type SocialLink } from "../../../types";
export interface Props {
  item: SocialLink;
  size?: number;
  alt?: string;
}
const { item, size = 24 } = Astro.props;
---

<item.icon size={size} />
