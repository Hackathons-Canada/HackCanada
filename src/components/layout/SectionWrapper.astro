---
import { cn } from "@/lib/utils";

const { elementType = "section", className, ...props } = Astro.props;
const SectionElement = elementType;
---

<SectionElement
  class={cn("sm:px-4 h-fit min-h-svh md:px-6 lg:px-16 w-full", className)}
  {...props}
>
  <slot />
</SectionElement>
