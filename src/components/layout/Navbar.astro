---
import { cn } from "@/lib/utils";

interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<header class={cn(className, "w-full h-20 py-3 fixed")}>
  <nav
    class="mx-auto max-w-screen-lg rounded-full border border-b-2 border-white bg-white/50 px-3 py-2 text-center text-lg text-black backdrop-blur-[20px]"
  >
    Navbar
  </nav>
</header>
