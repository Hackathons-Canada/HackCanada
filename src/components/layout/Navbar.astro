---
import { cn } from "@/lib/utils";

type Props = {
  className?: string;
};

const { className } = Astro.props;
---

<header class={cn("fixed flex items-center h-16 w-full border-b", className)}>
  <nav
    class="mx-auto flex w-full max-w-screen-xl items-center justify-between max-2xl:px-4"
  >
    <p class="text-xl font-semibold md:text-2xl">HackUSA</p>

    <!-- Nav Links - Desktop -->
    <ul class="flex items-center gap-16 max-lg:hidden">
      <li>Link</li>
      <li>Link</li>
      <li>Link</li>
      <li>Link</li>
    </ul>
  </nav>
</header>
