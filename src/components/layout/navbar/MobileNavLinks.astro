---
import { navLinks } from "@/config/links";
---

<ul class="flex flex-col gap-y-2.5 pb-4 pt-2 text-center">
  {
    navLinks.map((link) => (
      <li class="navlink rounded-lg bg-white/30 pb-1.5 pt-2 text-lg font-semibold">
        <a href={link.href}>{link.label}</a>
      </li>
    ))
  }
</ul>
