---
import { navLinks } from "@/config/links";
---

<ul class="flex flex-col gap-y-6 pb-6 pt-4 text-center">
  {
    navLinks.map((link) => (
      <li class="navlink text-xl font-semibold">
        <a href={link.href}>{link.label}</a>
      </li>
    ))
  }
</ul>
